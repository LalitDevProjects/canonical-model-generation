# generated by datamodel-codegen:
#   source: contracts/

from __future__ import annotations

from typing import Annotated
from uuid import UUID

from pydantic import AwareDatetime, BaseModel, ConfigDict, Field

from ...C1.SourceArtefact import _1_0 as field_1_0
from ...common import defs


class C4Corpusmanifest(BaseModel):
    """
    Transcribed from Section 3.4. The 'exclusions' key is required (may be an empty array) per the spec's own rule that a silently excluded source is indistinguishable from an absent one.
    """

    model_config = ConfigDict(
        extra='forbid',
    )
    runId: UUID
    domain: str
    sealedAt: AwareDatetime
    corpusHash: Annotated[str, Field(pattern='^[0-9a-f]{64}$')]
    """
    sha256 over the sorted artefact content hashes; MUST be stable across repeats over an unchanged corpus.
    """
    artefacts: list[field_1_0.C1Sourceartefact]
    exclusions: list[defs.ExclusionEntry]
